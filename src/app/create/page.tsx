import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Crear Página',
    alternates: {
        canonical: '/create',
    },
};

export { default } from './create-client';