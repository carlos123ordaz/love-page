import type { Metadata } from 'next';

export const metadata: Metadata = {
    title: 'Upgrade a PRO',
    alternates: {
        canonical: '/upgrade',
    },
};

export { default } from './upgrade-client';